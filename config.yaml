num_runs: 5
seed_base: 42

repro:
  deterministic: true

dataset:
  batch_size: 128

model:
  input_dim: 784
  hidden_dim: 256
  output_dim: 10
  dropout: 0.0

training:
  epochs: 3
  learning_rate: 0.001

paths:
  logs_dir: logs
  checkpoints_dir: checkpoints
  plots_dir: plots